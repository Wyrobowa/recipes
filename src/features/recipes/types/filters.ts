export type NutritionBounds = {
  kcal: number;
  protein: number;
  carbs: number;
  fat: number;
};

export type SliderRanges = {
  kcal: [number, number];
  protein: [number, number];
  carbs: [number, number];
  fat: [number, number];
};
