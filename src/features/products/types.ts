export type ApiProduct = {
  id?: number | string;
  _id?: number | string;
  name?: string;
  title?: string;
  slug?: string;
  description?: string;
};

export type Product = {
  id: number | string;
  name: string;
  slug: string;
};
