import React from 'react';

// Components
import Menu from '../../components/menu/Menu';

const Header = () => (
  <header>
    <h1>Recipe App</h1>
    <Menu />
  </header>
);

export default Header;
