export type ApiRecipe = {
  id: number | string
  name?: string
  title?: string
  description?: string
  instructions?: string
}

export type Recipe = {
  id: number | string
  name: string
  description: string
}
